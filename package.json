{
  "name": "football-schedule",
  "private": true,
  "type": "module",
  "workspaces": [
    "infra"
  ],
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "typecheck": "nuxt prepare && vue-tsc -b --noEmit",
    "infra:synth": "npm --workspace infra run synth"
  },
  "dependencies": {
    "nuxt": "4.5.2",
    "vue": "^3.5.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "vue-tsc": "^3.0.0"
  }
}
